# M0.1 五章终审

```text
日期：2026-09-04
对象：STAR-MONEY-CAPABILITY-CONTRACT rev0 · Part B
结论：当时 HOLD / UNSIGNED（签署前终审原文，不可回改缺口表）
随后升格：2026-09-05 签发 M0 FROZEN rev1（见 M0.1-ACCEPTANCE-WORKSHEET）

缺口：已写成确定值并被接受
覆盖：此前任何「能力已经对齐」结论作废
```

本文件是签署前终审对照，保留作不可变记录。签署后的状态以合同 rev1 为准。

```text
STAR 研究基础设施 = 部分实现
STAR 赚钱能力     = NO-EVIDENCE
M0-OBJECTIVE      = FROZEN
M0-MEASUREMENT    = HOLD / UNSIGNED
M1 / M2           = NOT STARTED
M3 / M4           = DENIED
M5 / M6           = DENIED
资金权限          = DENIED
```

---

## 总表

| 章 | 方向 | 待签确定值 | 可执行冻结 | 签署状态 |
| --- | --- | --- | --- | --- |
| B1 Portfolio | 接受 | 已写入 B1.5 | 待接受 | UNSIGNED |
| B2 Universe | 接受 | 已写入 B2.3 | 待接受 | UNSIGNED |
| B3 Execution | 接受 | 已写入 B3.3 / B3.5 | 待接受 | UNSIGNED |
| B4 Experiment | 接受 | 已写入 B4.1a | 待接受 | UNSIGNED |
| B5 Statistics | 接受 | 已写入 B5.4 | 待接受 | UNSIGNED |

五章都有可执行定义之后，仍须你逐条接受或改写，才能升为 `M0 FROZEN rev1`。未接受 = 继续 HOLD。

---

## 待签确定值

| ID | 确定值（写入主合同，尚未签署） |
| --- | --- |
| P-01 | 冻结/不可转账/可退出深度 0/单槽增发>10%：首次 observed_at 将未平仓记 0，`EXIT_IMPOSSIBLE`，不得删样本 |
| P-02 | 每日 00:00 UTC 用费用后 `mark_to_exit` 标记未平仓；已实现只在关闭时确认；复利用标记日净值 |
| P-03 | SOL/BTC 基准：窗起点按执行模型买入规范 USDC 池并持有；日切与期末按可实现卖出价；缺簿记不得补价 |
| P-04 | 现金只许 USDC；卖出所得 SOL 同一 as_of 换成 USDC |
| U-01 | pump.fun + Raydium AMM v4 / CPMM；报价 SOL/USDC（含 wSOL）；首次储备 ≥ 8 SOL 等值 |
| U-02 | P-01 或 24h 无成交且储备 < 1 SOL 等值 → `FAILED_RETAINED`，永留发现分母 |
| U-03 | 毕业 = 同一 mint 换场地，不是新项目；`first_tradable_at` = bonding-curve 首次可观测成交 |
| U-04 | 对照物 = 同一三程序、同一 slot 的二次回放；不可用则 M1-EVIDENCE 不得过 |
| E-01 | 部分成交名义 = 成交后报价侧仍 ≥ 80% 且冲击 ≤ 15% 的最大 USDC；剩余不得补价 |
| E-02 | 优先费 = 最近 150 slot 的 p75；可得 < 32 slot → `EXECUTION_FAILURE` |
| E-03 | 只用该 slot 链上曲线与费率；版本/费率未知 fail-closed；只询规范池 |
| E-04 | `executable_slot = max(signal, decision) + 2`；无簿记 → `NO_POINT_IN_TIME_BOOK` |
| E-05 | 闭合标签：`BUY_FAIL / PARTIAL_FILL / SELL_FAIL / EXIT_IMPOSSIBLE / NO_POINT_IN_TIME_BOOK / EXECUTION_FAILURE / FILL_OK` |
| X-01 | 按 `first_tradable_at`：60% TRAIN / 20% VAL / 14 日隔离 / 20% SEALED |
| X-02 | 读取 SEALED 合计或单 mint 结果，或写入决策者可见报告 = 已查看；必须记 `sealed_viewed_at` |
| X-03 | 查看后改规则：新实验 + 新版本 + **新封测窗**同时强制 |
| X-04 | 正式 M3：锁定 ACTIONABLE ≥ 200 且已关闭仓位 ≥ 80，否则 `INSUFFICIENT-EVIDENCE` |
| S-01 | 胜率分母 = 全部锁定 ACTIONABLE；失败与费用后亏损计未赢 |
| S-02 | 费用后科目：冲击、协议/LP 费、优先费、租金、失败费 |
| S-03 | Bootstrap：7 日块、10_000 次；观察窗 < 21 日不得出正式区间 |
| S-04 | 与 X-04 同一最低样本 |
| S-05 | 阶段用 TRAIN 的 7 日新 mint 数 p75/p25；SEALED 须各有 EXPANSION 与 COOLING，每段 ≥ 20 关闭仓且费用后净值 > 0 |

---

## 签署门槛（未变）

只有下面全部成立，才允许把状态改成 `M0-MEASUREMENT = FROZEN` 并签发 `M0 FROZEN rev1`：

1. 上表被接受为冻结值，或逐条改写后重新冻结。
2. 七条治理规则未被削弱。
3. 三个对象仍有可执行定义：交易、账户、实验。

在此之前：

```text
不签 rev1
不开发 M1 Recorder
不开发回测器 / 策略优化器 / 排行榜
不讨论 Micro-Live
赚钱能力 = NO-EVIDENCE
```
