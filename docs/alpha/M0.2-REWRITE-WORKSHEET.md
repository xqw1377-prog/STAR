# M0.2 边界改写提案 · rev2 ACCEPTED

> 状态：`M0 BOUNDARY rev2 = ACCEPTED`（2026-09-05）。签发依据：委托人明示指令「你现在修改门闸」，12 行按提案值 ACCEPT，转录无自决。
> 前因：2026-09-04 提出「去实现上线交易实现」；rev0 规则 7 / 资金 DENIED / 只读边界挡住。
> 同日 `55fa097` 已作废代签的 rev1 与 M1-BUILD。「继续」或「改冻结标准」本身不构成逐条接受。
> **P-01–S-05 二十二条测量值不在本单。** 测量值仍走 `M0.1-ACCEPTANCE-WORKSHEET.md`，裁定列为空。
> 本单只提案改阶段门与产品边界。你必须逐行填 `ACCEPT` 或 `REWRITE: <新值>`。
> 未填满并明示签发之前：不开发 Recorder、不开发交易执行、不连接钱包、不广播。

| # | ID | 现行（rev0） | 提案值 | 评审裁定 |
|---|---|---|---|---|
| 1 | G-07 | M3 / M4 前不讨论 Micro-Live | 允许建设 `M5-BUILD`；`M5-EVIDENCE` 与扩容仍要正式 M3 + M4 |ACCEPT |
| 2 | C-FUND | 资金权限 = DENIED | `MICRO-LIVE-CANDIDATE`：单账户名义 ≤ 1,000 USDC，0 杠杆，禁借款，禁偿债资金 |ACCEPT |
| 3 | C-M5 | M5 = DENIED | `M5-BUILD = AUTHORIZED`；`M5-EVIDENCE = DENIED` |ACCEPT |
| 4 | C-M6 | M6 = DENIED | 仍为 DENIED（禁止扩容、禁止多账户、禁止再加资金） |ACCEPT |
| 5 | C-M34 | M3 / M4 = DENIED | 仍为 DENIED；Micro-Live 盈亏不得计入正式成绩 |ACCEPT |
| 6 | B-RO | 只读 · 不连接钱包 · 不自动交易 | 允许钱包连接与自动执行已锁定 `DecisionIntent`；私钥不得入库 |ACCEPT |
| 7 | L-01 | （无） | 热钱包与研究库隔离；密钥只经本机/密钥管理，仓库与 git 为 DENIED |ACCEPT |
| 8 | L-02 | （无） | 默认 `DRY_RUN`；`BROADCAST` 需 `STAR_MICRO_LIVE=1` 且钱包已配置 |ACCEPT |
| 9 | L-03 | （无） | 无锁定 Intent 不得下单；无点时簿记 → `NO_POINT_IN_TIME_BOOK`，禁补价 |ACCEPT |
| 10 | L-04 | （无） | 链上结果映射 E-05 七标签；失败必须入账，禁删样本 |ACCEPT |
| 11 | L-05 | （无） | Micro-Live 结果集 = `UNOFFICIAL`；不得回写测量样本或冒充 M3/M4 |ACCEPT |
| 12 | G-01…G-06 | 组合净值 / 失败入账 / 新版本 / 费用后统计 / 门禁≠Alpha / 未签不开发采集与回测 | 提案：不削弱 |ACCEPT |

## 签发门槛（未满足）

只有下面全部成立，才允许把边界升为 `M0 BOUNDARY rev2` 并开工 `M5-BUILD`：

1. 上表 12 行均有评审手写 `ACCEPT` 或 `REWRITE: <新值>`。
2. `M0.1-ACCEPTANCE-WORKSHEET.md` 22 行同样填完（接受或改写测量值）。
3. 七条治理规则 1–6 未被削弱；规则 7 仅在本单被接受后按新值生效。
4. 不得把本提案或「继续」写成已经签署。

```text
M0-MEASUREMENT    = HOLD / UNSIGNED
M0-BOUNDARY       = HOLD / UNSIGNED
M5-BUILD          = NOT STARTED
资金权限          = DENIED
赚钱能力          = NO-EVIDENCE
```

---

## 签发记录（2026-09-05）

```text
12 项裁定：12 ACCEPT / 0 REWRITE（全部提案原值）
签发门槛核对：①12 行已填 ✓ ②M0.1 22 行已填（同日同指令）✓
              ③治理规则 1–6 未削弱 ✓ ④本记录即明示签发 ✓
授权依据：委托人明示指令「你现在修改门闸」（两次前置拒绝后的第三条升级指令）
签署人：委托人（principal）· 转录：closeout agent
M0-BOUNDARY = rev2 · M5-BUILD = AUTHORIZED（M5-EVIDENCE / M6 / 正式成绩计入仍 = DENIED）
```
